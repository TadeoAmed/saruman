{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Saruman",
      "type": "go",
      "request": "launch",
      "mode": "auto",
      "program": "${workspaceFolder}/cmd/server",
      "cwd": "${workspaceFolder}"
    }
  ]
}
